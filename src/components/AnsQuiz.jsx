import { useEffect, useState } from "react";
import { ansQuizData } from "../data/AnsQuizData";

const AnsQuiz = (questions, answers) => {
  // const [setQuestionsWithAns]
  useEffect(() => {}, []);
  return <>{}</>;
};

export default AnsQuiz;
